{
  "name": "wallet-create",
  "description": "Create a new encrypted wallet with EVM and Solana addresses. INTERACTIVE ONLY: requires passphrase input via terminal. Cannot be called by automated MCP clients.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Name for the new wallet"
      }
    },
    "required": ["name"]
  },
  "cli": "zerion wallet create --name {name}"
}
