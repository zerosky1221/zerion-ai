{
  "name": "search",
  "description": "Search for tokens by name or symbol. Returns token details including market data.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Token name or symbol to search for (e.g. PEPE, uniswap)"
      },
      "chain": {
        "type": "string",
        "description": "Filter by chain (e.g. ethereum, base)"
      }
    },
    "required": ["query"]
  },
  "cli": "zerion search {query} [--chain {chain}]"
}
