{
  "name": "wallet-transactions",
  "kind": "wallet-capability",
  "method": "GET",
  "path": "/v1/wallets/{address}/transactions/",
  "auth": "Basic",
  "description": "Returns interpreted transactions for a wallet.",
  "input": {
    "address": {
      "type": "string",
      "required": true
    },
    "page[size]": {
      "type": "integer",
      "required": false,
      "description": "Number of transactions to return per page."
    },
    "filter[chain_ids]": {
      "type": "string",
      "required": false
    }
  },
  "responseShape": {
    "data": {
      "type": "array",
      "itemAttributes": "interpreted transaction metadata and actions"
    }
  },
  "limitations": [
    "Solana transactions do not currently support NFT transactions.",
    "This endpoint supports many filters and pagination parameters."
  ],
  "exampleRequest": "GET https://api.zerion.io/v1/wallets/0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045/transactions/?page[size]=10",
  "exampleResponseNotes": [
    "Used for recent wallet activity summaries in the skill and CLI."
  ],
  "source": "https://developers.zerion.io/reference/listwallettransactions"
}
